<h3>Fields</h3>
<%= link_to 'Add Fields', new_form_field_path(@form) %>
<% if fields.empty? %>
  <p>No fields</p>
<% else %>
  <ul>
    <% fields.each do |field| %>
      <% unless field.outdated? %>
        <li><%= render 'field', field: %></li>
      <% end %>
    <% end %>
  </ul>
<% end %>
